# ld65 config: emit kernel.bin as a flat binary that loads at $4000.
# The MIA fast loader copies these bytes to $4000 and runs them from there.
MEMORY {
    RAM: start = $4000, size = $1000, fill = no, file = %O;
}

SEGMENTS {
    CODE: load = RAM, type = ro, start = $4000;
}
